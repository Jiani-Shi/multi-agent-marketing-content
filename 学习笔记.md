marketing_content_generator/
├── marketing_env/              # 虚拟环境（不提交）
├── data/
│   ├── raw/                    # 原始数据
│   └── vectors/                # FAISS索引
├── src/
│   ├── prompts/
│   │   ├── __init__.py
│   │   ├── base.py
│   │   ├── douyin.py
│   │   └── xiaohongshu.py
│   ├── __init__.py
│   ├── config.py
│   ├── vector_store.py
│   ├── retriever.py
│   ├── router.py
│   ├── generator.py
│   └── evaluator.py
├── app.py
├── requirements.txt
├── README.md
└── .env

# 创建虚拟环境
>> python -m venv marketing_env

# 激活虚拟环境
>> marketing_env\Scripts\activate

# 创建根目录下的文件夹
>> mkdir data\raw; mkdir data\vectors; mkdir src\prompts

# 安装依赖
>> pip install -r requirements.txt

# 直接安装requirements.txt容易因为依赖版本冲突而失败，正确办法是
1. 先逐个安装
2. python -c "import langchain, faiss, gradio, numpy, pandas; print('✅ 所有依赖安装成功！')" 
3. pip freeze > requirements.txt 把当前环境的真实版本导出

# 重新构建向量库
>> python -m src.vector_store